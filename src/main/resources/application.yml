spring:
  application:
    name: sb-ecom

  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/ecommerce
    password: root
  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true